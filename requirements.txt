# Runtime dependencies. Installed locally.
httpx>=0.27,<1.0          # BSD-3-Clause: HTTP client (tests + injectable mocks)
pydantic>=2.0,<3.0        # MIT: schema validation
curl_cffi>=0.7,<1.0       # MIT: Chrome TLS impersonation for bot-walled hosts
playwright>=1.40,<2.0     # Apache-2.0: Chromium for JS / cookie challenges
beautifulsoup4>=4.12,<5.0 # MIT: parse server-rendered comparison tables

# After install: ``python -m playwright install chromium``
# Set ODDS_HTTP_PROXY when an exit IP is geo-blocked.
# Set ODDS_FETCH_MODE=browser to force the Playwright path globally.

# Storage (sqlite3), timezones (zoneinfo), and hashing (hashlib) come from the
# standard library.
