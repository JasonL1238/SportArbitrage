# Runtime dependencies. All permanently free, open source, and installed
# locally — no accounts, API keys, hosted services, or paid tiers.
httpx>=0.27,<1.0        # BSD-3-Clause: HTTP client
pydantic>=2.0,<3.0      # MIT: schema validation

# Storage (sqlite3), timezones (zoneinfo), and hashing (hashlib) come from the
# standard library, so there is no database server or extra package to install.
